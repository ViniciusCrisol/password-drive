import React from 'react';
import { FcKey } from 'react-icons/fc';

import { Container } from './styles';

const Logo: React.FC = () => {
  return (
    <Container>
      <FcKey size={36} />
    </Container>
  );
};

export default Logo;
